<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ray.lab.mapper.AccountMapperA">

    <update id="updateDollar" parameterType="Account">
        UPDATE dollar SET money = #{money}  WHERE user_id=#{userId}
    </update>

    <update id="updateRMB" parameterType="Account">
        UPDATE rmb SET money = #{money}  WHERE user_id=#{userId}
    </update>

</mapper>